# Serve static SEO files directly (Netlify serves files before rewrites,
# but these explicit rules act as extra safety)
/robots.txt    /robots.txt    200
/sitemap.xml   /sitemap.xml   200

# Common typo redirect
/sitemaps.xml  /sitemap.xml   301

# SPA fallback for all other routes
/*    /index.html   200